{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The desktop loading window. The document editor does not receive Tauri shell permissions.",
  "windows": ["main-*"],
  "permissions": ["core:default"]
}
